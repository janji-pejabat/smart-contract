# Rust Toolchain Configuration for Paxi Smart Contracts
# This file forces the use of Rust nightly to support edition2024 dependencies
# Place this file in the root of your repository

[toolchain]
channel = "nightly"
components = ["rustfmt", "clippy"]
targets = ["wasm32-unknown-unknown"]
profile = "minimal"